const supertest = require('supertest');

const app = require('../app');

const request = supertest(app);

describe('API  Spotify', () => {
    test('Deve retornar XXX e um XXXX no VERBO /ROTA', async () => {});
})